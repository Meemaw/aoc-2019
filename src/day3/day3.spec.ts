import { partOne, partTwo } from './day3';

describe('day1', () => {
  it('Should solve part one', () => {
    expect(partOne()).toEqual(386);
  });

  it('Should solve part two', () => {
    expect(partTwo()).toEqual(6484);
  });
});

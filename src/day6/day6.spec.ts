import { partOne, partTwo } from './day6';

describe('day6', () => {
  it('Should solve part one', () => {
    expect(partOne()).toEqual(224901);
  });

  it('Should solve part two', () => {
    expect(partTwo()).toEqual(334);
  });
});

import { partOne, partTwo } from './day4';

describe('day4', () => {
  it('Should solve part one', () => {
    expect(partOne()).toEqual(921);
  });

  it('Should solve part one', () => {
    expect(partTwo()).toEqual(603);
  });
});

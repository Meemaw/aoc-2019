import { partOne, partTwo } from './day5';

describe('day5', () => {
  it('Should solve part one', () => {
    expect(partOne()).toEqual(15097178);
  });

  it('Should solve part one', () => {
    expect(partTwo()).toEqual(1558663);
  });
});

import { partOne, partTwo } from './day2';

describe('day1', () => {
  it('Should solve part one', () => {
    expect(partOne()).toEqual(4138687);
  });

  it('Should solve part one', () => {
    expect(partTwo()).toEqual(6635);
  });
});
